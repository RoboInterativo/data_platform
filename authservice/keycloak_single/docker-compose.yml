version: '3.8'

services:
  back:
    # network_mode: host
    build: ./back
  openresty:
    # network_mode: host
    build:  ./openresty
    command: -c /nginx.conf

    volumes:
      - ./conf/nginx.conf:/nginx.conf

  postgres1:
    user: "999"
    image: postgres:16.9
    build: .
    container_name: postgres1
    hostname: postgres1
    # network_mode: host
    environment:
      - POSTGRES_USER=access_service
      - POSTGRES_PASSWORD=strong_pass
      - POSTGRES_DB=access_service
      # DB_USER="access_service"
      # DB_NAME="access_service"
      # DB_PASSWORD="strong_pass"
      # LINUX_USER="access_service"

    volumes:
      - postgresql_data:/var/lib/postgresql/data
    # network_mode: host

  # openldap:
  #   image: bitnami/openldap:2.5.16
  #   user: root
  #   container_name: openldap
  #   # network_mode: host
  #   restart: always
  #   environment:
  #
  #
  #     - BITNAMI_DEBUG=true
  #     # - LDAP_ADMIN_PASSWORD=adminpassword
  #     # - LDAP_ROOT=dc=example,dc=com
  #
  #     - LDAP_ROOT=dc=example,dc=com
  #     - LDAP_ADMIN_USERNAME=admin
  #     - LDAP_ADMIN_PASSWORD=adminpassword  # Только этот пароль!
  #
  #     # - LDAP_ROOT=dc=example,dc=com
  #     # - LDAP_ADMIN_USERNAME=admin
  #     # - LDAP_ADMIN_PASSWORD=adminpassword  # Используйте ТОЛЬКО этот пароль
  #     # - LDAP_USERS=user1,user2
  #     # - LDAP_PASSWORDS=password1,password2
  #     # - LDAP_ROOT=dc=example,dc=com
  #     # - LDAP_ADMIN_USERNAME=admin
  #     # - LDAP_ADMIN_PASSWORD=adminpassword
  #     # - LDAP_USERS=user1,user2
  #     # - LDAP_PASSWORDS=password1,password2
  #     # - LDAP_ROOT_PASSWORD=rootpassword
  #   # ports:
  #   #   - "389:1389"
  #   #   - "636:1636"
  #   volumes:
  #     - openldap_data:/bitnami/openldap
  #     # - /opt/ldap:/bitnami/openldap



  keycloak-node1:
    # image: bitnami/keycloak:26.2.5-debian-12-r3
    image: "bitnami/keycloak:26.2.5"

    container_name: keycloak-node1
    # hostname: keycloak
    environment:
      - KC_HTTPS_ENABLED=true
      - KC_HTTP_ENABLED=true  # С редиректом на HTTPS
      - KC_HOSTNAME_STRICT_HTTPS=false
      - KC_PROXY=edge
      - KEYCLOAK_HTTPS_CERTIFICATE_FILE=/certs/tls.crt
      - KEYCLOAK_HTTPS_CERTIFICATE_KEY_FILE=/certs/tls.key
      - KEYCLOAK_ADMIN_USER=admin
      - KEYCLOAK_ADMIN_PASSWORD=adminpassword
      - KEYCLOAK_DATABASE_HOST=postgres1
      - KEYCLOAK_DATABASE_PORT=5432
      - KEYCLOAK_DATABASE_NAME=access_service
      - KEYCLOAK_DATABASE_USER=access_service
      - KEYCLOAK_DATABASE_PASSWORD=strong_pass
      # - KC_LOG_LEVEL=DEBUG
      # - JAVA_OPTS=-Dorg.jboss.logging.provider=slf4j

    ports:
      - "8443:8443"
      - "8080:8080"
    volumes:
      - /opt/certs:/certs  # Подключаем сертификаты
    # depends_on:
    #   - postgresql1

    restart: always


  # loadbalancer:
  #   image: nginx:latest
  #   container_name: loadbalancer
  #   # ports:
  #   #   - "80:80"
  #   volumes:
  #     - ./nginx.conf:/etc/nginx/nginx.conf
  #     - /var/www/docs:/var/www/docs
  #   # depends_on:
  #   #   - keycloak-node1
  #   #   - keycloak-node2
  #   network_mode: host

volumes:
  openldap_data:
    driver: local
  postgresql_data:
    driver: local

#networks:
#  keycloak_network:
#    driver: bridge
